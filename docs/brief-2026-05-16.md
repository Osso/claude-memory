# claude-memory Brief - 2026-05-16

> Historical planning note. The analyzer, memory-unit runtime, and graph paths
> referenced below were later retired; see `docs/wiki/systems/retrieval-flows.md`.

## Superseded Tasks

- [ ] Add notability frame + scope classification to extractor prompt in `analyze.rs`.
  - [ ] Extractor outputs `project: Option<String>` — `None` for global patterns, `Some(cwd-slug)` for project-local facts.
  - [ ] Notability frame: see `docs/plans/memory-units-notability-frame.md` for the adapted codealmanac prompt.
- [ ] Extend enrich to filter memory-unit retrieval by project slug (project-local memories only surface in matching cwd).

## Progress This Session

- Reviewed codealmanac repo (`~/Repos/codealmanac`) — TypeScript CLI that maintains AI-generated wikis inside `.almanac/` per repo.
- Identified that codealmanac's notability + purpose prompts (not the code) are the actual product. Key insight: "intelligence in prompts, not pipelines."
- Historical MCP behavior (superseded): `memory_write` and `memory_list` had a `project` field; typed semantic search was global.
- Confirmed the friction-memory pipeline already exists: `analyze.rs`, `backfill.rs`, `memory_unit.rs`, `dedup.rs`, `extract.rs`.
- Identified two real gaps by reading source: (1) `analyze.rs` extractor sets `project: None` always, no notability frame; (2) `enrich_cmd.rs` has no project filtering.

## Key Decisions

- **Q12 (locked):** Extractor LLM classifies scope — global (cross-project pattern) vs. project-local (cwd-specific fact). `project` field set by LLM at extraction time, not hardcoded. Enrich filters by project slug at retrieval time.
- **Q13 (locked):** Extractor uses codealmanac notability frame as foundation: same notability categories (entity, flow, contract, decision, risk/invariant, incident, research synthesis), same anti-patterns, same core test ("costly, useful, or risky to reconstruct"). Adapted for 1-3 sentence preloads, not wiki pages.
- Both decisions appended to `AgentConfig/rules/2026-04-28-analyzer-and-memory-units.md` locked decisions table.
- `MemoryUnit` schema in design doc updated to add `project: Option<String>` with note on retrieval behavior.

## Next Steps

1. Read `src/analyze.rs` — find the extractor LLM prompt, add notability frame from `docs/plans/memory-units-notability-frame.md`, add scope classification output (`global` / `project:<slug>`), wire into `MemoryUnit.project`.
2. Read `src/enrich_cmd.rs` — add project slug detection (cwd basename or configured slug), pass to memory-unit search filter.
3. Run tests after each change.

## Technical Context

### What exists in the pipeline (DO NOT re-implement)
- `memory_unit.rs` — `MemoryUnit` struct already has `project: Option<String>` field
- `analyze.rs` — friction classifier + candidate extractor + replay validator + dual judge already implemented
- `backfill.rs` — backfill command already exists
- `dedup.rs` — cosine dedup at write already implemented
- `extract.rs` — transcript extraction utilities

### The two gaps
1. **`analyze.rs` extractor prompt** — currently sets `project: None` always. Need to:
   - Add notability frame to prompt (file: `docs/plans/memory-units-notability-frame.md`)
   - Add scope classification: LLM outputs whether the preload is global or project-local
   - Parse that output and set `MemoryUnit.project` accordingly
2. **`enrich_cmd.rs`** — currently passes `project: None` to memory retrieval. Need to:
   - Detect current project (cwd basename, or configurable slug)
   - Pass as filter: project-local memories only surface in matching cwd, global memories always surface

### Design doc location
`/home/osso/AgentConfig/rules/2026-04-28-analyzer-and-memory-units.md` — full pipeline design + all locked decisions (Q1–Q13).

### Notability frame reference
`/home/osso/Projects/claude/claude-memory/docs/plans/memory-units-notability-frame.md` — adapted codealmanac notability prompt ready to paste into extractor.

### codealmanac reference
Cloned to `~/Repos/codealmanac`. Prompts at `prompts/base/notability.md` and `prompts/base/purpose.md` are the source.
